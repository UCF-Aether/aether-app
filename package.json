{
  "name": "aether-app",
  "version": "1.0.0",
  "author": "Paul Wood",
  "scripts": {
    "start": "source .env && pnpm run start --filter !infra",
    "build": "sst build",
    "deploy": "sst deploy",
    "start:infra": "sst start --console"
  },
  "dependencies": {
    "@serverless-stack/cli": "0.60.11",
    "@serverless-stack/resources": "0.60.11",
    "esbuild": "^0.14.25"
  }
}
