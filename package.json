{
  "name": "aether-app",
  "version": "1.0.0",
  "author": "Paul Wood",
  "scripts": {
    "start": "exit 0",
    "build": "sst build",
    "deploy": "sst deploy",
    "deploy:prod": "sst deploy --stage prod",
    "build:prod": "sst build --stage prod",
    "infra:start": "sst start --console"
  },
  "dependencies": {
    "@serverless-stack/cli": "0.60.11",
    "@serverless-stack/resources": "0.60.11"
  }
}
