query Readings($chan: String!, $after: Datetime = "2000-01-01") {
  readingsWithin(filter: {takenAt: {greaterThanOrEqualTo: $after}}, chan: $chan) {
    totalCount
    nodes {
      val
      geog {
        latitude
        longitude
      }
      chan
      deviceId
      takenAt
    }
  }
}
